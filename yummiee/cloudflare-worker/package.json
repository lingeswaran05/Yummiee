{
  "name": "yummiee-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test/test-runner.js",
    "db:migrate:local": "wrangler d1 migrations apply yummiee-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply yummiee-db --remote"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "jose": "^6.0.8"
  },
  "devDependencies": {
    "wrangler": "^3.114.0"
  }
}
